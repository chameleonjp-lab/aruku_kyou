# アルク教 index.html 改善 実装計画書

作成日: 2026-06-19
対象: `index.html`（単一ファイル）
方式: Opusのみで4役割に分担しテスト → 課題抽出 → 役割横断で擦り合わせ → 採否決定 → 実装
**絶対制約: 画面文言は一切変更しない。README仕様（ゲーム化禁止・外部通信禁止・λ象徴維持・格言60個維持）を正本とする。**

---

## 1. テスト体制（役割分担）

| 役割 | 担当領域 | 実施者 | 状態 |
|------|----------|--------|------|
| ビジュアル/UI | デザイン・配色・余白・装飾一貫性 | Opusサブエージェント | 完了（D-01〜D-10） |
| UX/操作性 | 導線・ナビ・タップ・フィードバック | Opusサブエージェント | 完了（U-01〜U-13） |
| パフォーマンス | 読み込み・CLS・実行時負荷 | Opus本体 | 完了（P-01〜P-07） |
| アクセシビリティ/QA | a11y・堅牢性・仕様適合 | Opus本体 | 完了（A-01〜A-09） |

※ 仕様適合は検証済み: 格言60個・空なし / 13セクション / 外部通信・Supabase・ランキング・個人情報入力なし / `lang="ja"` / 見出し階層正常。

---

## 2. 役割横断の擦り合わせ（重複課題の統合）

複数役割が同一根を指摘した課題をクラスタ化し、最適解を1つに収れんさせた。

### クラスタA：下部固定UIの交通整理
- 関連: U-01（進捗UI3〜4系統併存）, D-07（スマホでcounterとpagerが中央下で重なる）, A-02（counterのlive連呼）, P-04（ドット毎回再生成）
- 擦り合わせ: 「歩み N/13」を出すUIが walk-counter / pager / counter内ミニドット で重複。スマホでは物理的に重なる。
- **採用解**: ①counter内ミニドットを廃止（数字＋live役は残す）②ドットは初回生成のみ・以後トグル③数字は`<strong>`のtextContentのみ更新（live連呼抑制）④スマホ(≤699px)は重複するcounterを非表示にしpagerへ一本化。

### クラスタB：移動UIとタップ領域
- 関連: U-04（pagerボタン40px<44px）, U-03（disabled表現が弱い）, U-02（移動後に下部barが本文を隠す）
- **採用解**: pagerボタン`min-height:44px`化／disabledをgrayscale等で明確化＋hover/activeフィードバック追加／`.walk-section`へ`scroll-margin-top`を付与し着地余白確保（最下部はpadding-bottom既存7.2remで担保済み）。

### クラスタC：入口ボタン体験
- 関連: U-05（860ms無反応・多重押下）
- **採用解**: 多重押下ガード＋押下中の視覚フィードバック（is-steppingでボタンを沈める）＋ディレイ860→640ms（足跡アニメ.62s直後）。

### クラスタD：読み込み・性能
- 関連: P-01（CLS）, P-07（will-change）, P-04（上記Aで解決）
- **採用解**: シンボル画像に`width/height`付与（CLS解消）／touch-footprintの`will-change`から`left/top`除外。

### クラスタE：アクセシビリティ堅牢化
- 関連: A-01（装飾nav）, A-04（不正role=list）, A-06/U-06（footprintのreduced-motion非対応・読書妨害）
- **採用解**: stone-navに`aria-hidden="true"`／FAQの`role="list"`削除／footprintをreduced-motionで非表示＋通常時opacity .9→.55。

### クラスタF：ヒーロー可読性
- 関連: D-04（catchコピーが沈む）
- **採用解**: 色トークンは金のまま、`text-shadow`と僅かな字間で主役感と可読性を補強。

---

## 3. 今回の実装スコープ（採用）

| # | 由来 | 内容 | 種別 | リスク |
|---|------|------|------|--------|
| 1 | P-01 | シンボル`<img>`に`width="1024" height="1024"` | HTML属性 | 低 |
| 2 | A-04 | FAQ `role="list"` 削除 | HTML属性 | 低 |
| 3 | A-01 | stone-nav `aria-hidden="true"` | HTML属性 | 低 |
| 4 | U-01/A-02/P-04 | counterミニドット廃止＋数字のみ更新 | JS | 低 |
| 5 | P-04 | pagerドットを初回のみ生成しトグル | JS | 低 |
| 6 | U-05 | stepButton多重押下ガード＋フィードバック＋遅延640ms | JS | 低 |
| 7 | D-04 | `.catch`にtext-shadow＋字間 | CSS | 低 |
| 8 | U-04 | pagerボタン`min-height:44px` | CSS | 低 |
| 9 | U-03 | pager disabled強調＋hover/active | CSS | 低 |
| 10 | U-05 | `body.is-stepping #stepButton`沈み込み | CSS | 低 |
| 11 | U-02 | `.walk-section{scroll-margin-top:1rem}` | CSS | 低 |
| 12 | P-07 | footprint `will-change`からleft/top除外 | CSS | 低 |
| 13 | U-06/A-06 | footprint opacity .55＋reduced-motion非表示 | CSS | 低 |
| 14 | U-01/D-07 | スマホ(≤699px)でwalk-counter非表示 | CSS | 低 |

CSS（7〜14）は1つの追記ブロックに集約し、レビュー容易性を確保。JS/HTMLは該当箇所をピンポイント修正。

---

## 4. 据え置き（今回は実装せず計画書に記録）

| 由来 | 内容 | 据え置き理由 |
|------|------|--------------|
| D-01/D-02/D-10/P-05 | CSS多重定義の単一ソース化 | 1537行の積層上書きを盲目的に撤去すると描画が変わる恐れ。視覚回帰検証（DevTools実値記録）を伴う別タスクとすべき。 |
| D-03 | card二重背景の一本化 | 同上、視覚リスク。 |
| P-02 | 195KB背景画像の整理/再圧縮 | 演出維持・画像加工ツール非確認のため別途。 |
| U-07 | 本文→ホーム復帰導線 | 新ラベル＝文言追加が必要で「文言不変」制約に抵触。仕様判断事項。 |
| D-05/A-09/A-10 | meter象徴性・死にロジック・冗長ARIA | 影響軽微・無害。 |

---

## 5. 検証方法

- 文言不変の確認: 変更前後で表示テキストの差分が無いこと（HTML本文・60格言・FAQ・ボタンラベル）。
- 構造健全性: 格言60個・13セクション・外部通信なしが維持されていること。
- 機能確認: ページャー前後移動／格言再表示／λコピー／FAQ開閉／stepButton導線が動作。
- 回帰防止: 変更はすべて加算的（属性付与・新規CSSブロック・JSの最小書換）。
